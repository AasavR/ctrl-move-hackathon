import React from 'react';
export default function Merchant(){
  return (<div>
    <h2 className="text-lg">Merchant Dashboard (demo)</h2>
    <p>Scan QR code to receive payment (demo stub)</p>
  </div>);
}
